package com.notepad.app.data.database.migrations

import androidx.room.migration.Migration

/**
 * Explicit Room migrations starting from schema v7 baseline.
 * Add new [Migration] objects here when bumping [com.notepad.app.data.database.NoteDatabase] version.
 */
object Migrations {
    val ALL: Array<Migration> = emptyArray()
}
